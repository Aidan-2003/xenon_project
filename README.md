"# xenon_project" 
The folder named "main_code_used" contains basically all the code that was used to get the results actually presented in the thesis, alongside "conors_plugins", of which model.py has been altered to adjust the models corresponding to how I used them for this thesis. 

Any .ipynb files or .py files that are not included in conor_plugins or main_code_used, are likely very large files with nothing actually important in them that I was using previously but dropped later, either I am still searching through them to find if there is anything relevant or they will be deleted soon.

The folder named "plots" includes all the plots that were made by the code in main_code_used
